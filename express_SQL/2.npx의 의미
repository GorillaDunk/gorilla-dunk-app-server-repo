지금까지 
npx nodemon app.js
npx sequelize init
npx sequelize db:create --env development
라는 명령어들을 실행했었다. 

npx는 패키지에 내장된 명령어를 실행하기 위해 사용하는 키워드이다. 

npx sequelize를 실행하게되면 node_modules 디렉토리 안에 있는 .bin 디렉토리 안의 sequelize파일을 실행하게된다. 
이때 이 sequelize 파일은 바로가기 파일이라서 그 원본 파일인 node_modules 디렉토리 안에 있는 sequelize-cli패키지 안의 lib디렉토리 안의 sequelize파일을 node로 실행하기된다. 